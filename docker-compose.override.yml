services:
  backend:
    volumes:
      - ./app:/app/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"

  frontend:
    profiles:
      - prod  # excluded from default `docker compose up` in dev
